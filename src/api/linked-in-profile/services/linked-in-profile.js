'use strict';

/**
 * linked-in-profile service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::linked-in-profile.linked-in-profile');
